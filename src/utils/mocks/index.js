const { deliveriesMock, DeliveriesServiceMock } = require('./deliveries');
const { driversMock } = require('./drivers');
const { postDeliveries } = require('./postDeliveries');

module.exports = {
  deliveriesMock,
  DeliveriesServiceMock,
  driversMock,
  postDeliveries,
};
